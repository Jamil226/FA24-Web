const numbers = [45, 4, 9, 16, 25];

let text = "";
for (let x in numbers) {
    text += numbers[x];
}
console.log(text);

// const numbers_array = [45, 4, 9, 16, 25];
// for(let a in numbers_array){
    
// }

